<table width="100%" class="table table-striped table-bordered table-hover">
  <thead>
      <tr>
          <th>#</th>
          <th>ID</th>
          <th>Name</th>
          <th>E-mail</th>
          <th colspan=2 style="width: 10%"></th>
      </tr>
  </thead>
  <tbody>
      <% @users.each_with_index do |user, index| %>
          <tr class=<%= even_odd(index) %>>
              <td><%= index + 1%></td>
              <td><%= user.id %></td>
              <td><%= user.name %></td>
              <td><%= user.email %></td>
              <td>
                  <%= link_to edit_backoffice_user_path(user), class: "btn btn-outline btn-primary btn-block" do %>
                      <i class="fa fa-edit"></i>
                  <% end %>
              </td>
              <td>
                  <%= link_to backoffice_user_path(user), class: "btn btn-outline btn-danger btn-block", method: :delete, data: { title: 'Are you sure?', type: "warning", confirm: "You won't be able to revert this!" } do %>
                      <i class="fa fa-trash-o"></i>
                  <% end %>
              </td>
          </tr>
      <% end %>
      <tr>
          <td colspan=6>
              <%= link_to new_backoffice_user_path, class: "btn btn-outline btn-success btn-block" do %>
                  <i class="fa fa-plus"></i>
              <% end %>
          </td>
      </tr>
  </tbody>
</table>