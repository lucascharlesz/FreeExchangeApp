<%= form_with(model: backoffice_user, local: true) do |form| %>
  <% if backoffice_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(backoffice_user.errors.count, "error") %> prohibited this backoffice_user from being saved:</h2>

      <ul>
      <% backoffice_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
